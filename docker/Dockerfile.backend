FROM python:3.10-slim
RUN pip install --no-cache-dir fastapi uvicorn numpy
WORKDIR /app
COPY backend /app/backend
ENV ODOM_TOPIC=/odom CMD_TOPIC=/cmd_vel
EXPOSE 8001
CMD ["uvicorn","backend.server:app","--host","0.0.0.0","--port","8001"]
